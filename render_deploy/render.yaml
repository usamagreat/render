services:
  - type: web
    name: safewatch-relay
    env: python
    rootDir: render_deploy/backend
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: SAFEWATCH_TOKEN
        generateValue: true
